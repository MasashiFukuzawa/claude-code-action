import type { PreparedContext } from "./types";

export async function createOrchestratorPrompt(
  _context: PreparedContext,
  _taskDescription: string,
): Promise<string> {
  // TODO: Implement orchestrator prompt generation
  return "";
}